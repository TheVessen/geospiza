{
  "name": "charthopper",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "esbuild src/index.ts --bundle --format=iife --global-name=ChartHopper --outfile=../Resources/charthopper.js",
    "watch": "esbuild src/index.ts --bundle --format=iife --global-name=ChartHopper --outfile=../Resources/charthopper.js --watch"
  },
  "devDependencies": {
    "@types/plotly.js": "^3.0.6",
    "esbuild": "^0.27.3"
  }
}
